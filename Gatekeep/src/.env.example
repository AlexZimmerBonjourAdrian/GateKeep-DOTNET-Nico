# Configuración Docker de GateKeep
# Copia este archivo a .env y configura tus valores reales
# IMPORTANTE: Nunca subas el archivo .env al control de versiones

# ============================================
# PostgreSQL
# ============================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=Gatekeep
DB_USER=postgres
DB_PASSWORD=tu_contraseña_segura_aqui

# ============================================
# MongoDB (opcional - para notificaciones y auditoría)
# ============================================
# Si usas MongoDB Atlas, usa la cadena de conexión completa
# Si usas MongoDB local, usa: mongodb://localhost:27017
MONGODB_CONNECTION=mongodb+srv://user:pass@host/?appName=GateKeepMongo
MONGODB_DATABASE=GateKeepMongo
MONGODB_USE_STABLE_API=true

# ============================================
# Redis
# ============================================
REDIS_CONNECTION=redis:6379
REDIS_INSTANCE=GateKeep:
REDIS_ENABLED=true

# ============================================
# API
# ============================================
APP_ENVIRONMENT=Production
GATEKEEP_PORT=5011

# ============================================
# JWT (Seguridad)
# ============================================
# IMPORTANTE: Usa una clave secreta de al menos 256 bits (32 caracteres mínimo)
# Genera una clave segura: openssl rand -base64 32
JWT_KEY=clave-secreta-muy-larga-minimo-256-bits-cambiar-en-produccion
JWT_ISSUER=GateKeep
JWT_AUDIENCE=GateKeepUsers
JWT_EXPIRATION_HOURS=8

# ============================================
# Security (Configuración de seguridad)
# ============================================
SECURITY_PASSWORD_MIN_LENGTH=8
SECURITY_MAX_LOGIN_ATTEMPTS=5
SECURITY_LOCKOUT_DURATION_MINUTES=15

# ============================================
# Grafana (Opcional - para monitoreo)
# ============================================
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
